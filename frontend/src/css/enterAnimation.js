import { keyframes } from "styled-components";

const enterAnimation = keyframes`
0% {
    transform: scale(0);
    opacity: 1;
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
`

export default enterAnimation